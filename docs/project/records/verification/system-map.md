---
id: VER-system-map
kind: verification
title: 系统地图扩展检查与阅读实测
status: current
summary: 2026-09-16，91 项自动化检查通过；实测系统目录、护照呈现和目标项目页面。内嵌鼠标点击仍受自动化坐标限制。
relations:
  - relation: verifies
    to: {record_id: IMP-system-map}
  - relation: verifies
    to: {record_id: REQ-system-map}
---

# 系统地图扩展检查与阅读实测

检查日期：2026-09-16。环境：Windows，Python 3.11.5（本机 Anaconda），Node.js 24；代码为本次工作区修改，未以已发布版本代称。

## 自动化

`python -m pytest tests maintain-project-map/tests -q`：91 passed。新增覆盖明确收录、不递归扩展、同一接口多个消费者、交叉循环边、两系统共用项目、分页与嵌套消费者截断、歧义工作树、目标图缺失、模块范围、无关变更后续读和合同变化拒绝旧续读。护照项目入口另外检查新窗口目标、断开 opener、阻止打开时的反馈和源图选择/相机保留。

真实本机 HTTP 测试覆盖系统入口到完整项目阅读页的跳转、图和节点参数、未收录项目/无效节点/非法路径参数拒绝。已有普通地图、历史/回退、文档链接、画布交互及预览测试继续通过。示例系统图通过 Archify 原生 9/9 项交付检查，0 错误、0 警告。

本 Skill 自身地图现有 58 条记录，结构检查 0 错误、0 警告；自身架构图与流程图均通过原生交付检查，架构图仍有 3 项布局警告，流程图 0 警告。68 个捆绑上游文件逐项 SHA-256 核对无改动。

## 浏览器观察

- 系统显示系统概览、接口与关联、收录项目、更新记录；不显示功能路径。
- 接口目录展示两类合同，同一引用合同下有两个消费者及其接入链接。
- 选中系统图项目后出现原生语义护照及“进入项目”；原页未直接导航。
- 收录项目入口已在新标签页打开 Reference Core、Note Board 的完整阅读页，页面有目标项目自己的侧栏和整体组织图，普通项目保留功能路径。
- 护照项目链接触发后，目标 Graph View 的阅读文件和本机预览已生成；原图护照与选择保留。浏览器自动化标签清单未可靠报告该新标签页，所以不将此项记为完整可视端到端通过。

## 未覆盖和限制

内嵌 iframe 的鼠标点击被工具以 `Cannot safely inspect fractional iframe input coordinates` 拒绝；键盘可触发护照，完整图可点击，但还不能据此宣布内嵌鼠标和切回标签页的全部状态已逐项实测。系统真实分组未迁移，ThoughtDAG 等真实项目验收仍应在建立对应系统后进行。

Windows 隐藏启动由启动参数和渲染链检查支持；没有独立桌面录像证明所有第三方环境绝不会弹出窗口。没有进行 MRS、性能、token 成本或后续开发质量 benchmark。
