---
id: VER-build
kind: verification
title: 首版构建检查
status: partial
summary: 44 项功能测试、安装核对及独立试用已完成；浏览器视觉与长期对照未覆盖。
relations:
- relation: verifies
  to:
    record_id: IMP-release
sources:
- path: ../../research/first-build-check.md
  role: historical-verification
---

# 首版构建检查

日期：2026-09-15。以下保留首版构建检查；随后默认 HTTP 入口修订的 7 项专项检查及全部 51 项回归结果见 [VER-http-preview](http-preview.md)。验证状态为 partial，因为未覆盖浏览器视觉和长期效果。

## 实际检查

- 组合运行 `python -m pytest maintain-project-map/tests tests -q`：44 项通过。其中地图引擎 25 项、阅读和图形 14 项、会话来源 5 项。
- 地图引擎覆盖既有文档绑定、长记录分段及版本变化、退役/合并、外部 ID、多位置解析、两个独立进程并发登记、无变化不重写、跨盘路径回退。
- 阅读检查覆盖实际 Archify 架构/流程导出、中文与原文快照、输出不得覆盖来源、图形失败不冒充新图、跨项目同 ID 隔离、历史别名和探索状态。阅读器脚本语法检查通过。
- 会话脚本覆盖中段中文匹配、长消息分段、原生身份去重、文件位置回退，排除内部推理和工具数据。
- Skill 官方结构检查在源目录和安装目录均通过。Windows 默认编码影响验证器读取中文，使用 Python UTF-8 模式完成检查。
- 安装核对：43 个交付文件，约 1.94 MB，逐文件 SHA-256 与源目录一致；未安装测试夹具、临时任务或开发调研材料。安装版脚本已实际执行登记、解析、检索、分段读取和阅读导出，并修正导出命令在 Windows 中的中文回执编码。
- 本项目 ID 已经由安装版脚本登记并成功解析。首版地图当时包含 12 条记录；架构包含 4 个模块，显式阅读生成流程包含 3 个节点。

## 独立前向使用

一名不带本轮分析上下文的子智能体，仅获得本 Skill、合成项目和一个自然语言修复/历史查询任务。实际修复同名材料覆盖，保留来源映射与输入原件，并根据合成 EVT-03 解释“分享卡片”是功能退役。未实施提议中的语义检索，未冒称外部消费者已核查。

根代理检查改动：6 个已有文件变化、2 个新文件，均关联该任务；退役记录、失败探索、提议和合成历史保持原样。根代理重跑真实 CLI 的三个冲突场景，全部通过。详见 [试用记录](../../../../research/first-build-check.md)。

## 未覆盖

2026-09-15 阅读布局修订（REQ-reader-layout）：固定视口布局、独立滚动区域及侧栏滚动位置保留已写入模板，安装版与源文件一致；Node 的阅读脚本语法检查通过，实际地图重新导出成功。浏览器连接探测返回 `nodeRepl.fetch request failed`，没有完成实际滚动、截图或窄屏检查。这次小范围样式修订未重跑全部功能测试。

此前浏览器工具对本地文件 URL 返回访问策略拒绝，没有通过其他方式绕过；未完成浏览器视觉、点击全流程和窄屏布局实测。自动测试与实际 HTML 生成不能代替这部分验收。

前向试用没有无 Skill 对照，没有模拟多轮长期开发、测量实际模型调用 token 或测试换会话收益；不能据一次成功宣称优于基线。未执行真实跨项目消费者联调，也未穷尽大图/异常输入。成本与长期评估设计仍独立保存，普通开发不自动运行。

## 原生 Archify 迁移后检查

2026-09-15，图模型、原生交付与 A/B/C 嵌入的修订检查另见 VER-native-archify；最终 58 项自动检查通过，安装同步与回执见该记录。本文件保留首版历史范围。
