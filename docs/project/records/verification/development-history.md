---
id: VER-development-history
kind: verification
title: 开发历程的检查范围
status: current
summary: 分类与任务两种入口扩展后，91 项自动检查通过；实测共享文档独立入口、刷新返回、证据展开、组合筛选与窄屏阅读。
relations:
  - relation: verifies
    to: {record_id: IMP-development-history}
    reason: 核对分层阅读、来源定位与既有阅读行为。
---

# 开发历程的检查范围

2026-09-17 在项目 Skill 源稿及同步安装版本上核对。

- 源稿阅读器与来源检索测试共 55 项通过，其中 11 项覆盖新的历程行为；地图核心另有 32 项通过。旧测试中仍引用已移除 C 视图和旧 URL 写入方式的夹具已更新到当前导航协议。
- 新检查覆盖公开字段白名单、调用配对、不完整范围、重复索引、来源与索引指纹改变、Unicode 分段还原、查询数量限制、候选查询不读取原文、导出不持久化工具载荷、断裂证据引用、原会话移走或改变后的明确失败，以及本机资源白名单。
- 官方 skill 格式检查通过；Windows 验证使用 UTF-8，核心测试显式配置脚本模块查找路径。
- 真实 MRS 任务索引为 126 个公开事件定位、59 组调用与返回，原文仍在原 Codex 会话。来源定位文件为 88702 字节；删除此前试做的 329 个重复原文导出片段。
- 浏览器实测原 HTML 侧栏顺序、过程正文、按需读取原会话、配对调用与返回、2000 字符后的继续读取、从当前实现返回后恢复已展开的依据及已读片段、无结果搜索与清除筛选。
- 390×844 窄屏下正文宽度 375、滚动宽度 375；展开的两段原文容器宽度与滚动宽度均为 291，无横向溢出。临时视口已恢复；检查时未发现浏览器错误。

自动检查并不证明以后所有过程摘要都忠实，整理时仍需引用真实来源并标注未记录的原因。没有重新运行 B 题数学研究或全套研究阅读验证，本次改变属于项目地图的开发历程能力。原会话删除后的正文继续可读、原文不可读由测试覆盖，不在真实会话上执行删除。

## 分类与任务入口扩展的核查

2026-09-17 后续扩展提供“按问题查找”和“按任务回顾”。以下是这次新增行为的检查范围，前面的 87 项是上一阶段记录。

- 阅读器及来源检索共 59 项检查、地图核心 32 项检查通过，总计 91 项。新增检查覆盖经验摘要的分类、结果、模块与分页，多个分类不重复返回，来源丢失仍能查询叙述，共用父任务索引，以及无效任务、类别、结果和证据引用阻止导出。
- 使用实际目录代码检查同一经验的分类入口互不影响、只选中一个入口、修复无效入口参数、旧任务与新经验链接、任务视角单独定位。不同入口共用同一记录，未复制正文。
- 在自身地图实际打开“人工纠偏”下的共享经验，“改进”保持关闭；刷新后相同，当前入口只有一个。切换到任务全程，再切回问题视角，所选分类及已展开依据恢复；顶栏返回恢复先前的任务页。
- 较矮桌面窗口中，侧栏滚动至 158 后用键盘打开经验，渲染后仍为 158；其他分类没有自动展开。
- 390×844 窄屏实测分类筛选只返回对应的一条经验，正文 clientWidth 和 scrollWidth 均为 375，没有横向溢出。检查结束已恢复视口。
- 自身地图 67 条记录和 MRS 维护地图 44 条记录通过结构及引用校验，两者 HTML 已重建。自身任务新增 38 个公开来源定位、17 组完整调用，原文继续留在宿主会话。MRS 两条经验复用原有泳道任务的索引。

经验中的“曾未通过”和“验证通过”可同时保留，表示先前尝试及复验的不同结果；当前筛选不把最终成功覆盖为从未失败。没有为这次分类扩展新增过程图或重新评估数学结论。

## 主动维护提示的核对

2026-09-17 后续修订仅涉及 Skill 提示和本地图记录，未改动查询、存储或阅读器代码。入口明确新建和实质更新时同步历程，详细参考覆盖同任务续写、小改动、纯导出、经验提炼、来源缺失与非递归记录；保留模型自主查询和原文按需读取。

- Skill 格式检查通过，本次修改的两份指引通过定向差异空白检查，源稿与安装版本逐文件哈希一致。
- 自身地图 69 条记录通过校验，无错误和警告；开发历程验证通过，包含 2 项任务、3 条经验。
- 本次新增来源只保存 17 个公开事件定位，7 组调用与返回完整配对，没有复制原始消息或工具输出。索引范围截至首次规则修改、同步安装和格式检查，不把后续整理写入这份来源范围。

没有为纯提示修改重跑旧版 91 项代码测试；此前测试结果仍只证明当时实现。提示生效依赖模型加载并遵循 Skill，本次检查不宣称所有未来任务都会自动执行。
