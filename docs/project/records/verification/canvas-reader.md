---
id: VER-canvas-reader
kind: verification
title: 画布操作与视口适配的检查范围
status: partial
summary: 文档内点选修订后，72 项自动检查通过，含 6 项选中状态和 8 项相机检查；浏览器视觉仍未验收。
relations:
  - relation: verifies
    to: {record_id: REQ-canvas-reader}
  - relation: verifies
    to: {record_id: IMP-release}
---

# 画布操作与视口适配的检查范围

## 当前：文档内点选修订

2026-09-15，恢复原 A/B/C 文档布局。默认文档滚动，点击图形窗口才选中画布，点击外部退出。回执适配版本为 project-map-inline-canvas-v2。

- tests 下的 40 项检查全部通过，36.36 秒；Skill 引擎的 32 项检查全部通过，22.15 秒，共 72 项。
- 新增 6 项选中层检查执行交付的 canvas-interaction.js：默认 iframe 不接收鼠标事件且选择层不注册滚轮/触摸移动拦截；首次点击仅选中；点击外部和焦点离开退出；多图互斥；退出按钮与父文档 Escape；重新渲染清理状态和避免重复监听。激活和退出不改变 iframe 地址，不重载图。
- 保留 8 项真实 Archify 相机代码检查；阅读导出和节点定位检查继续通过。
- 只读复核确认 renderContent 在替换正文前清理选择状态，C 的延迟展开也经过同一个选择层。36px 状态行位于图形外，iframe 沿用原文档高度，不改成全主面板画布。
- 源目录与安装目录的 89 个交付文件逐一一致；68 个上游原文件与 PIN 哈希一致。

浏览器检查补充：全量浏览器发现接口返回 nodeRepl.fetch request failed，但指定既有 browser 1 后成功取得标签页、刷新、点击和截图。CUA 的全量 ListBrowsers 会强制 refresh，已知浏览器的标签页路径可复用已有连接；不能由发现接口失败推断浏览器控制全部不可用。具体刷新请求的底层错误码未取得。

实际页面检查了 C 长文档、流程图展开、未选中提示、点击后选中、外部点击取消选中及正文滚动（取消选中后正文 scrollTop 从 1330 变为 1090）。图形窗口保留在文档流中，顶部和侧栏保持可见。跨 iframe 输入检查遇到 Cannot safely inspect fractional iframe input coordinates，无法完成受控的图内拖动与纯滚轮缩放验收；未将高层滚动工具的自动聚焦行为当作普通用户滚轮的证明。临时视口已恢复。当前仍缺完整鼠标手感、大图和窄屏检查。

事件替身验证代码状态，不模拟浏览器布局或完整输入分发。iframe 内的 Escape 不会到达父文档；退出操作以点击外部或“返回文档”为准。

## 此前：相机扩展检查

以下保留修订前的历史范围，其中整块主面板画布的布局未被用户接受，已恢复文档布局；这些结果不能证明后续点选逻辑。此前组合检查 66 项全部通过，耗时 50.92 秒。

### 实际检查

新增 tests/test_canvas_camera.py 的 8 项检查提取真实生成副本中的 Archify.view 控制器执行，使用可控的尺寸与事件替身，覆盖：默认概览状态和保持比例、指针锚点连续缩放、控制面板滚轮保留、100% 及更小比例的拖动、节点/关系点击不被抢占、5%–800% 边界、适应全图、尺寸改变后的概览恢复及手动位置保持、窄窗口语义镜头，以及关闭 canvas 后与上游行为一致。

阅读界面检查验证 canvas URL、原版链接独立、多节点定位和 C 按需展开。架构与流程实际导出成功，原生检查分别为 9/9、错误和警告为 0；其原版文件和浅色画布副本各自保存哈希。两份图和主页面的全部 JavaScript 通过语法检查。

源目录通过官方 Skill 结构检查。上游运行源文件保持不变，修改发生于独立的下游画布适配脚本和生成副本；交付回执中适配版本为 project-map-fixed-light-canvas-v1。

### 未覆盖

本轮浏览器控制工具返回 nodeRepl.fetch request failed，未取得浏览器视图。因此以上不代表已完成真实 CSS 排版、窄屏截图或鼠标手感验收。测试中的尺寸替身检查数学和交互状态，不模拟浏览器布局引擎。
